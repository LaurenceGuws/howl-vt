const ffi = @import("src/ffi/main.zig");

pub const FfiBytesResult = ffi.FfiBytesResult;
pub const FfiFeedResult = ffi.FfiFeedResult;
pub const FfiVisibleMetaResult = ffi.FfiVisibleMetaResult;
pub const FfiRuntimeObligationResult = ffi.FfiRuntimeObligationResult;
pub const FfiRuntimeProgressResult = ffi.FfiRuntimeProgressResult;
pub const FfiSelectionResult = ffi.FfiSelectionResult;
pub const FfiRenderStateHandle = ffi.FfiRenderStateHandle;
pub const FfiRowIteratorHandle = ffi.FfiRowIteratorHandle;
pub const FfiRowCellsHandle = ffi.FfiRowCellsHandle;
pub const FfiDirty = ffi.FfiDirty;
pub const FfiCursorVisualStyle = ffi.FfiCursorVisualStyle;
pub const FfiData = ffi.FfiData;
pub const FfiOption = ffi.FfiOption;
pub const FfiRowData = ffi.FfiRowData;
pub const FfiRowOption = ffi.FfiRowOption;
pub const FfiRowCellsData = ffi.FfiRowCellsData;
pub const FfiRowSelection = ffi.FfiRowSelection;
pub const FfiRowHighlight = ffi.FfiRowHighlight;
pub const FfiColors = ffi.FfiColors;
pub const HowlVtCallStatus = ffi.HowlVtCallStatus;

pub const terminalInit = ffi.terminalInit;
pub const terminalInitWithOptions = ffi.terminalInitWithOptions;
pub const terminalDeinit = ffi.terminalDeinit;
pub const terminalResize = ffi.terminalResize;
pub const terminalSetCellPixelSize = ffi.terminalSetCellPixelSize;
pub const terminalAckSurface = ffi.terminalAckSurface;
pub const terminalStartSelection = ffi.terminalStartSelection;
pub const terminalFeed = ffi.terminalFeed;
pub const terminalCopyTitle = ffi.terminalCopyTitle;
pub const terminalQueryVisibleMeta = ffi.terminalQueryVisibleMeta;
pub const terminalQuerySelection = ffi.terminalQuerySelection;
pub const terminalQueryRuntimeObligation = ffi.terminalQueryRuntimeObligation;
pub const terminalProgressRuntime = ffi.terminalProgressRuntime;
pub const terminalEncodeKey = ffi.terminalEncodeKey;

pub const renderStateInit = ffi.renderStateInit;
pub const renderStateDeinit = ffi.renderStateDeinit;
pub const renderStateUpdate = ffi.renderStateUpdate;
pub const renderStateAck = ffi.renderStateAck;
pub const renderStateGet = ffi.renderStateGet;
pub const renderStateGetMulti = ffi.renderStateGetMulti;
pub const renderStateSet = ffi.renderStateSet;
pub const renderStateColorsGet = ffi.renderStateColorsGet;
pub const renderStateRowIteratorInit = ffi.renderStateRowIteratorInit;
pub const renderStateRowIteratorDeinit = ffi.renderStateRowIteratorDeinit;
pub const renderStateRowIteratorNext = ffi.renderStateRowIteratorNext;
pub const renderStateRowGet = ffi.renderStateRowGet;
pub const renderStateRowGetMulti = ffi.renderStateRowGetMulti;
pub const renderStateRowSet = ffi.renderStateRowSet;
pub const renderStateRowCellsInit = ffi.renderStateRowCellsInit;
pub const renderStateRowCellsDeinit = ffi.renderStateRowCellsDeinit;
pub const renderStateRowCellsNext = ffi.renderStateRowCellsNext;
pub const renderStateRowCellsSelect = ffi.renderStateRowCellsSelect;
pub const renderStateRowCellsGet = ffi.renderStateRowCellsGet;
pub const renderStateRowCellsGetMulti = ffi.renderStateRowCellsGetMulti;
